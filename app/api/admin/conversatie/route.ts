import {NextRequest,NextResponse} from "next/server";import {authorized} from "../invitatii/route";import {attachMeetingRoom,proposeCoffeeMeeting,reactToConversationMessage,readConversation,respondCoffeeMeeting,sendConversationMessage} from "../../../../lib/conversation";import {readInvitation} from "../../../../lib/invitations";import {createPrivateRoom,createRoomAccess,meetingRoomName} from "../../../../lib/metered";
export const runtime="nodejs";export const dynamic="force-dynamic";const valid=(id:string|null)=>!!id&&/^[0-9a-f-]{36}$/i.test(id);async function target(req:NextRequest){const id=req.nextUrl.searchParams.get("id");if(!valid(id))return null;const invitation=await readInvitation(id!);return invitation?{id:id!,invitation}:null}
export async function GET(req:NextRequest){if(!authorized(req))return NextResponse.json({error:"Unauthorized"},{status:401});const row=await target(req);return row?NextResponse.json({conversation:await readConversation(row.id)},{headers:{"Cache-Control":"private, no-store"}}):NextResponse.json({error:"Invitația nu există."},{status:404})}
export async function POST(req:NextRequest){if(!authorized(req))return NextResponse.json({error:"Unauthorized"},{status:401});const row=await target(req);if(!row)return NextResponse.json({error:"Invitația nu există."},{status:404});try{const body=await req.json();return NextResponse.json({conversation:await sendConversationMessage(row.id,"adrian",String(body.text||""),body.kind)})}catch(error){return NextResponse.json({error:(error as Error).message},{status:400})}}
export async function PATCH(req:NextRequest){if(!authorized(req))return NextResponse.json({error:"Unauthorized"},{status:401});const row=await target(req);if(!row)return NextResponse.json({error:"Invitația nu există."},{status:404});try{const body=await req.json();if(body.action==="react")return NextResponse.json({conversation:await reactToConversationMessage(row.id,String(body.messageId||""),"adrian",String(body.emoji||""))});if(body.action==="propose")return NextResponse.json({conversation:await proposeCoffeeMeeting(row.id,"adrian",String(body.startsAt||""),Number(body.plannedMinutes))});if(body.action==="respond")return NextResponse.json({conversation:await respondCoffeeMeeting(row.id,body.status)});if(body.action==="join"){let conversation=await readConversation(row.id);if(!conversation?.meeting||conversation.meeting.status!=="acceptata")throw new Error("Cafeaua în Doi nu este confirmată.");let roomName=conversation.meeting.roomName;if(!roomName){roomName=meetingRoomName(row.id,conversation.meeting.id);await createPrivateRoom(roomName);conversation=await attachMeetingRoom(row.id,roomName)}return NextResponse.json({conversation,call:await createRoomAccess(roomName,"Adrian",true)})}return NextResponse.json({error:"Acțiune invalidă."},{status:400})}catch(error){return NextResponse.json({error:(error as Error).message},{status:400})}}
