import {NextRequest,NextResponse} from "next/server";
import crypto from "crypto";
import {consumeRateLimit,requestIp,resetRateLimit} from "../../../../lib/rate-limit";
import {ADMIN_COOKIE,ADMIN_SESSION_SECONDS,createAdminSession} from "../../../../lib/admin-auth";
function same(a:string,b:string){const left=Buffer.from(a),right=Buffer.from(b);return left.length===right.length&&crypto.timingSafeEqual(left,right)}
export async function POST(req:NextRequest){const ip=requestIp(req),limit=await consumeRateLimit("admin-login",ip,5,15*60_000,{failClosed:true});if(!limit.allowed){const res=NextResponse.redirect(new URL("/admin/login?error=rate",req.url),303);res.headers.set("Retry-After",String(limit.retryAfter));return res}const fd=await req.formData(),email=String(fd.get("email")||"").trim().toLowerCase(),password=String(fd.get("password")||""),next=String(fd.get("next")||"/admin");const adminEmail=(process.env.ADMIN_EMAIL||process.env.INVITATION_TO_EMAIL||"").trim().toLowerCase(),adminPassword=process.env.ADMIN_PASSWORD||"";if(!adminEmail||!adminPassword||!same(email,adminEmail)||!same(password,adminPassword))return NextResponse.redirect(new URL("/admin/login?error=1",req.url),303);await resetRateLimit("admin-login",ip);const res=NextResponse.redirect(new URL(next.startsWith("/admin")?next:"/admin",req.url),303);res.cookies.set(ADMIN_COOKIE,createAdminSession(),{httpOnly:true,secure:true,sameSite:"strict",path:"/",maxAge:ADMIN_SESSION_SECONDS});return res}
export async function DELETE(){const r=NextResponse.json({ok:true});r.cookies.set(ADMIN_COOKIE,"",{httpOnly:true,secure:true,sameSite:"strict",path:"/",maxAge:0});return r}
