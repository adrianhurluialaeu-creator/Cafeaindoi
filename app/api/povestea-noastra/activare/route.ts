import {NextRequest,NextResponse} from "next/server";
import {activatePortalAccount,createPortalSession,PORTAL_COOKIE} from "../../../../lib/portal-auth";
import {updatePortalAccess} from "../../../../lib/invitations";
import {consumeRateLimit,requestIp,resetRateLimit} from "../../../../lib/rate-limit";
import {sendPush} from "../../../../lib/push";
export const runtime="nodejs";
export async function POST(req:NextRequest){try{const ip=requestIp(req),limit=await consumeRateLimit("portal-activation",ip,7,15*60_000);if(!limit.allowed)return NextResponse.json({error:"Prea multe încercări. Încearcă din nou mai târziu."},{status:429,headers:{"Retry-After":String(limit.retryAfter)}});const {id,token,password}=await req.json();if(typeof id!=="string"||!/^[0-9a-f-]{36}$/i.test(id)||typeof token!=="string"||token.length>100||typeof password!=="string"||password.length<10||password.length>128)return NextResponse.json({error:"Parola trebuie să aibă între 10 și 128 de caractere."},{status:400});if(!await activatePortalAccount(id,token,password))return NextResponse.json({error:"Linkul este invalid sau a expirat."},{status:400});await resetRateLimit("portal-activation",ip);await updatePortalAccess(id,"active");try{await sendPush("Cont activat","Contul privat a fost activat.",{url:"/admin/invitatii",tag:`activare-${id}`})}catch(error){console.error("[push] ACTIVATION_ERROR",error)}const res=NextResponse.json({ok:true});res.cookies.set(PORTAL_COOKIE,createPortalSession(id),{httpOnly:true,secure:true,sameSite:"strict",path:"/",maxAge:7*86400});return res}catch{return NextResponse.json({error:"Activarea nu a reușit."},{status:400})}}
