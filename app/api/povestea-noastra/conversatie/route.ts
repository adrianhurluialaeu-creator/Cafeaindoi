import {cookies} from "next/headers";import {NextResponse} from "next/server";import {attachMeetingRoom,proposeCoffeeMeeting,reactToConversationMessage,readConversation,respondCoffeeMeeting,sendConversationMessage} from "../../../../lib/conversation";import {allowConversationMutation,trustedConversationOrigin} from "../../../../lib/conversation-security";import {readInvitation} from "../../../../lib/invitations";import {createPrivateRoom,createRoomAccess,meetingRoomName} from "../../../../lib/metered";import {PORTAL_COOKIE,verifyPortalSession} from "../../../../lib/portal-auth";
export const runtime="nodejs";export const dynamic="force-dynamic";async function current(){return verifyPortalSession((await cookies()).get(PORTAL_COOKIE)?.value)}async function identity(){const id=await current();if(!id)return null;const invitation=await readInvitation(id);return invitation?.portalAccessStatus==="active"?{id,invitation}:null}
export async function GET(){const user=await identity();return user?NextResponse.json({conversation:await readConversation(user.id)},{headers:{"Cache-Control":"private, no-store"}}):NextResponse.json({error:"Unauthorized"},{status:401})}
export async function POST(req:Request){const user=await identity();if(!user||!trustedConversationOrigin(req))return NextResponse.json({error:"Unauthorized"},{status:401});const limit=await allowConversationMutation(req,user.id);if(!limit.allowed)return NextResponse.json({error:"Prea multe acțiuni. Încearcă din nou într-un minut."},{status:429,headers:{"Retry-After":String(limit.retryAfter)}});try{const body=await req.json();return NextResponse.json({conversation:await sendConversationMessage(user.id,"ea",String(body.text||""),body.kind)})}catch(error){return NextResponse.json({error:(error as Error).message},{status:400})}}
export async function PATCH(req:Request){const user=await identity();if(!user||!trustedConversationOrigin(req))return NextResponse.json({error:"Unauthorized"},{status:401});const limit=await allowConversationMutation(req,user.id);if(!limit.allowed)return NextResponse.json({error:"Prea multe acțiuni. Încearcă din nou într-un minut."},{status:429,headers:{"Retry-After":String(limit.retryAfter)}});try{const body=await req.json();if(body.action==="react")return NextResponse.json({conversation:await reactToConversationMessage(user.id,String(body.messageId||""),"ea",String(body.emoji||""))});if(body.action==="propose")return NextResponse.json({conversation:await proposeCoffeeMeeting(user.id,"ea",String(body.startsAt||""),Number(body.plannedMinutes))});if(body.action==="respond")return NextResponse.json({conversation:await respondCoffeeMeeting(user.id,String(body.status||"") as "acceptata"|"refuzata"|"anulata")});if(body.action==="join"){let conversation=await readConversation(user.id);if(!conversation?.meeting||conversation.meeting.status!=="acceptata")throw new Error("Cafeaua în Doi nu este confirmată.");let roomName=conversation.meeting.roomName,attach=false;if(!roomName){roomName=meetingRoomName(user.id,conversation.meeting.id);attach=true}await createPrivateRoom(roomName);if(attach)conversation=await attachMeetingRoom(user.id,roomName);return NextResponse.json({conversation,call:await createRoomAccess(roomName,user.invitation.prenume,false)})}return NextResponse.json({error:"Acțiune invalidă."},{status:400})}catch(error){return NextResponse.json({error:(error as Error).message},{status:400})}}
