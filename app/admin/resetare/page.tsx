import Link from "next/link";
import AdminResetPasswordForm from "../../../components/AdminResetPasswordForm";
export const metadata={title:"Schimbare parolă",robots:{index:false,follow:false}};
export default async function Page({searchParams}:{searchParams:Promise<{token?:string;error?:string}>}){const p=await searchParams,error=p.error==="password"?"Parolele trebuie să coincidă și să aibă minimum 12 caractere.":p.error==="rate"?"Prea multe încercări. Încearcă mai târziu.":p.error?"Linkul este invalid, a expirat sau a fost deja folosit.":"";return <main className="story admin-login"><div className="eyebrow">Cafea în Doi</div><h1>Alege parola nouă</h1><p>Introdu aceeași parolă în ambele câmpuri.</p>{error&&<p className="formerror" role="alert">{error}</p>}{p.token?<AdminResetPasswordForm token={p.token}/>:<p><Link href="/admin/recuperare">Solicită un link nou</Link></p>}</main>}
