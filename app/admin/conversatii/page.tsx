import AdminConversationHub from "../../../components/AdminConversationHub";import AdminShell from "../../../components/AdminShell";import {readConversation} from "../../../lib/conversation";import {listInvitations} from "../../../lib/invitations";
export const metadata={title:"Conversații · Admin",robots:{index:false,follow:false}};export const dynamic="force-dynamic";
export default async function Page({searchParams}:{searchParams:Promise<{id?:string}>}){const active=(await listInvitations()).filter(item=>item.portalAccessStatus==="active"),items=await Promise.all(active.map(async item=>{const conversation=await readConversation(item.id),last=conversation?.messages.at(-1);return{id:item.id,prenume:item.prenume,localitate:item.localitate,tara:item.tara,lastMessage:last?.text,lastActivityAt:last?.createdAt,expiresAt:conversation?.expiresAt,meetingStatus:conversation?.meeting?.status}})),selectedId=(await searchParams).id||"";return <AdminShell title="Conversații" subtitle="Mesagerie privată și întâlniri video Cafea în Doi."><AdminConversationHub items={items} selectedId={selectedId}/></AdminShell>}
