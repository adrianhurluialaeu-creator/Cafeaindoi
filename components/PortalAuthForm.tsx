"use client";
import {FormEvent,useState} from "react";
import {useRouter} from "next/navigation";
export function PortalLoginForm(){const router=useRouter(),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const fd=new FormData(e.currentTarget),r=await fetch("/api/povestea-noastra/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:fd.get("email"),password:fd.get("password")})}),x=await r.json();setBusy(false);if(!r.ok)return setError(x.error);router.push("/povestea-noastra");router.refresh()}return <form className="portal-auth-form" onSubmit={submit}>{error&&<p className="formerror" role="alert">{error}</p>}<label>Email<input name="email" type="email" required autoComplete="username"/></label><label>Parolă<input name="password" type="password" required autoComplete="current-password"/></label><button className="btn" disabled={busy}>{busy?"Se verifică…":"Intră în Povestea noastră"}</button></form>}
export function PortalActivationForm({id,token}:{id:string;token:string}){const router=useRouter(),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget),password=String(fd.get("password")||""),confirm=String(fd.get("confirm")||"");if(password!==confirm)return setError("Parolele nu coincid.");setBusy(true);setError("");const r=await fetch("/api/povestea-noastra/activare",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({id,token,password})}),x=await r.json();setBusy(false);if(!r.ok)return setError(x.error);router.push("/povestea-noastra");router.refresh()}return <form className="portal-auth-form" onSubmit={submit}>{error&&<p className="formerror" role="alert">{error}</p>}<label>Alege parola<input name="password" type="password" minLength={10} maxLength={128} required autoComplete="new-password"/><small>Minimum 10 caractere.</small></label><label>Repetă parola<input name="confirm" type="password" minLength={10} maxLength={128} required autoComplete="new-password"/></label><button className="btn" disabled={busy}>{busy?"Se activează…":"Activează contul"}</button></form>}
