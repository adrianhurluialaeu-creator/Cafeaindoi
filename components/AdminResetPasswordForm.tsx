"use client";
import {useState} from "react";
export default function AdminResetPasswordForm({token}:{token:string}){const [visible,setVisible]=useState(false),type=visible?"text":"password";return <form method="post" action="/api/admin/resetare"><input type="hidden" name="token" value={token}/><label htmlFor="password">Parolă nouă</label><input id="password" name="password" type={type} minLength={12} maxLength={128} required autoComplete="new-password"/><small>Minimum 12 caractere.</small><label htmlFor="confirm">Repetă parola nouă</label><input id="confirm" name="confirm" type={type} minLength={12} maxLength={128} required autoComplete="new-password"/><label className="password-visibility"><input type="checkbox" checked={visible} onChange={event=>setVisible(event.target.checked)}/><span>{visible?"Ascunde parolele":"Arată parolele"}</span></label><button className="btn" type="submit">Schimbă parola și intră în Admin</button></form>}
