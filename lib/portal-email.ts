const safe=(value:string)=>value.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]!));
export async function sendPortalActivationEmail(input:{email:string;name:string;invitationId:string;token:string}){const key=process.env.RESEND_API_KEY_V2,from=process.env.INVITATION_FROM_EMAIL||"Cafea în Doi <onboarding@resend.dev>";if(!key)throw new Error("Configurația de email este incompletă.");const url=`https://www.cafeaindoi.eu/povestea-noastra/activare?id=${encodeURIComponent(input.invitationId)}&token=${encodeURIComponent(input.token)}`;const text=`Bună, ${input.name}!\n\nAdrian ți-a oferit acces la spațiul privat „Povestea noastră”. Activează-ți contul folosind linkul de mai jos. Linkul este valabil 7 zile.\n\n${url}\n\nDacă nu dorești să continui, poți ignora acest mesaj.`;const html=`<div style="font-family:Arial,sans-serif;max-width:600px;margin:auto;color:#33241f"><h2>☕ Povestea noastră</h2><p>Bună, ${safe(input.name)}!</p><p>Adrian ți-a oferit acces la spațiul privat în care puteți continua traseul împreună.</p><p><a style="display:inline-block;padding:12px 18px;border-radius:10px;background:#4b2d1f;color:#fff;text-decoration:none;font-weight:bold" href="${url}">Activează pagina mea privată</a></p><p>Linkul este valabil 7 zile. Dacă nu dorești să continui, poți ignora mesajul.</p></div>`;const r=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({from,to:[input.email],subject:"Activează Povestea noastră · Cafea în Doi",text,html})});if(!r.ok)throw new Error("Invitația pentru cont nu a putut fi trimisă.")}
