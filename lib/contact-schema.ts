export type ContactStatus="new"|"in_progress"|"resolved"|"archived";
export type ContactSettings={enabled:boolean;intro:string;successMessage:string;email:string;phone:string;whatsapp:string;responseHours:string;facebook:string;instagram:string;tiktok:string};
export const defaultContactSettings:ContactSettings={enabled:true,intro:"Ai o întrebare despre site, invitații sau datele tale? Scrie-mi și îți răspund cât pot de repede.",successMessage:"Mesajul tău a fost trimis. Îți voi răspunde pe email.",email:"contact@cafeaindoi.eu",phone:"",whatsapp:"",responseHours:"De obicei răspund în maximum 48 de ore.",facebook:"",instagram:"",tiktok:""};
const text=(value:unknown,max:number)=>typeof value==="string"?value.trim().slice(0,max):"";
const url=(value:unknown)=>{const result=text(value,500);if(!result)return "";try{const parsed=new URL(result);return parsed.protocol==="https:"?result:""}catch{return ""}};
export function validateContactSettings(value:unknown):ContactSettings|null{if(!value||typeof value!=="object")return null;const v=value as Record<string,unknown>,enabled=typeof v.enabled==="boolean"?v.enabled:null,intro=text(v.intro,500),successMessage=text(v.successMessage,300),email=text(v.email,254),phone=text(v.phone,40),whatsapp=text(v.whatsapp,40),responseHours=text(v.responseHours,160),facebook=url(v.facebook),instagram=url(v.instagram),tiktok=url(v.tiktok);if(enabled===null||!intro||!successMessage||!responseHours||(email&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)))return null;return{enabled,intro,successMessage,email,phone,whatsapp,responseHours,facebook,instagram,tiktok}}
export function validateContactInput(value:unknown){if(!value||typeof value!=="object")return null;const v=value as Record<string,unknown>,name=text(v.name,80),email=text(v.email,254).toLowerCase(),subject=text(v.subject,140),message=text(v.message,5000),consent=v.consent===true,website=text(v.website,200);if(website)return{honeypot:true as const};if(name.length<2||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)||subject.length<2||message.length<10||!consent)return null;return{name,email,subject,message,consent}}
